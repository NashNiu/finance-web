# Loaded automatically by `npm run build` (Vite production mode).
# Relative path: the browser calls /api on the SAME origin that served the app,
# and nginx proxies /api to the backend. No hardcoded host, no CORS.
VITE_API_BASE_URL=/api
